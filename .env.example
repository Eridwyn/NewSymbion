# Configuration Symbion - Kernel
# Copiez ce fichier vers .env et modifiez les valeurs

# Clé API pour sécuriser l'accès aux endpoints (obligatoire)
SYMBION_API_KEY=s3cr3t-42

# Configuration MQTT (optionnel - par défaut depuis kernel.yaml)
# MQTT_BROKER=localhost
# MQTT_PORT=1883

# Configuration serveur (optionnel - par défaut 8080)
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8080

# Mode debug (optionnel - par défaut false)
# DEBUG_MODE=false