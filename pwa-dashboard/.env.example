# Configuration PWA Dashboard Symbion
# Copiez ce fichier vers .env et modifiez les valeurs

# Clé API pour se connecter au kernel Symbion
VITE_SYMBION_API_KEY=s3cr3t-42

# URL de base de l'API (optionnel - par défaut proxy Vite en dev)
# VITE_API_BASE_URL=http://localhost:8080

# Configuration MQTT WebSocket (optionnel - par défaut automatique)
# VITE_MQTT_BROKER=localhost
# VITE_MQTT_WS_PORT=9001